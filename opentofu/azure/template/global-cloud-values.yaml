# This file is auto-generated by OpenTofu (create_tf_resources) with real cloud values.
# The placeholder values below allow upgrade_chart.sh to run before infrastructure is provisioned.
# DO NOT use this template file for actual deployments — always use the generated file in your env directory.

global:
  env: dev
  environment: template
  building_block: sunbird
  cloud_storage_access_key: REPLACE_WITH_STORAGE_ACCOUNT_NAME
  cloud_storage_secret_key: REPLACE_WITH_STORAGE_ACCOUNT_KEY
  public_container_name: REPLACE_WITH_PUBLIC_CONTAINER_NAME
  private_container_name: REPLACE_WITH_PRIVATE_CONTAINER_NAME
  velero_storage_container_private: REPLACE_WITH_VELERO_CONTAINER_NAME
  private_ingressgateway_ip: 0.0.0.0
  encryption__string: REPLACE_WITH_ENCRYPTION_STRING
  random_string: REPLACE_WITH_RANDOM_STRING
  object_storage_endpoint: REPLACE_WITH_STORAGE_ACCOUNT_NAME.blob.core.windows.net
  storage_class: managed-premium
  cloud_storage_provider: azure
  cloud_storage_auth_type: "OIDC"
  azure_client_id: REPLACE_WITH_AZURE_CLIENT_ID
  workload_identity_service_account_name: REPLACE_WITH_SERVICE_ACCOUNT_NAME
  serviceAccount:
    create: false

velero:
  enabled: true
  credentials:
    useSecret: true
  configuration:
      backupStorageLocation:
        - name: default
          provider: "azure"
          bucket: "REPLACE_WITH_VELERO_CONTAINER_NAME"
          prefix:
          config:
            storageAccount: REPLACE_WITH_STORAGE_ACCOUNT_NAME
            useAAD: "true"
      dbbackupStorageLocation:
        - name: dbbackup
          provider: "azure"
          bucket: "REPLACE_WITH_VELERO_CONTAINER_NAME"
          prefix:
          config:
            storageAccount: REPLACE_WITH_STORAGE_ACCOUNT_NAME
            useAAD: "true"
      volumeSnapshotLocation:
        - name: default
          provider: "azure"
  initContainers:
  - name: velero-plugin-for-azure
    image: velero/velero-plugin-for-microsoft-azure:v1.14.0
    imagePullPolicy: IfNotPresent
    volumeMounts:
      - mountPath: /target
        name: plugins

nginx-public-ingress:
  commonAnnotations:
    reloader.stakater.com/auto: "true"
    service.beta.kubernetes.io/azure-load-balancer-external: "true"
