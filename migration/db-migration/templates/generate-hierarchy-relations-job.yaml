{{- if .Values.jobs.generateHierarchyRelations.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Values.jobs.generateHierarchyRelations.name }}
  namespace: {{ .Values.job.namespace }}
  labels:
    app: migration
    type: generate-hierarchy-relations
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
spec:
  backoffLimit: {{ .Values.job.backoffLimit }}
  ttlSecondsAfterFinished: {{ .Values.job.ttlSecondsAfterFinished }}
  completions: 1
  parallelism: 1
  template:
    metadata:
      labels:
        app: migration
        type: generate-hierarchy-relations
        release: {{ .Release.Name }}
    spec:
      restartPolicy: Never

      {{- if .Values.serviceAccount.create }}
      serviceAccountName: {{ .Values.serviceAccount.name }}
      {{- else }}
      serviceAccountName: default
      {{- end }}

      containers:
      - name: generate-hierarchy-relations
        image: "python:3.11-slim"
        imagePullPolicy: IfNotPresent

        command:
        - /bin/bash
        - -c
        - |
          apt-get update -qq && apt-get install -y -qq curl &&
          curl -sSL "https://dl.k8s.io/release/v1.28.0/bin/linux/amd64/kubectl" -o /tmp/kubectl &&
          chmod +x /tmp/kubectl && mv /tmp/kubectl /usr/local/bin/kubectl &&
          python3 /scripts/generate_hierarchy_relations.py

        env:
        - name: YB_POD
          value: "{{ .Values.generateHierarchyRelations.ybPod }}"
        - name: YB_NAMESPACE
          value: "{{ .Values.generateHierarchyRelations.ybNamespace }}"
        - name: KEYSPACE
          value: "{{ .Values.global.env }}_hierarchy_store"
        - name: TABLE
          value: "{{ .Values.generateHierarchyRelations.table }}"
        - name: KNOWLG_SERVICE_HOST
          value: "{{ .Values.generateHierarchyRelations.knowlgServiceHost }}"
        - name: KNOWLG_SERVICE_PORT
          value: "{{ .Values.generateHierarchyRelations.knowlgServicePort }}"
        - name: API_DELAY_SECONDS
          value: "{{ .Values.generateHierarchyRelations.apiDelaySeconds }}"
        - name: API_TIMEOUT_SECONDS
          value: "{{ .Values.generateHierarchyRelations.apiTimeoutSeconds }}"
        - name: PROGRESS_EVERY
          value: "{{ .Values.generateHierarchyRelations.progressEvery }}"
        - name: DRY_RUN
          value: "{{ .Values.generateHierarchyRelations.dryRun }}"

        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"

        volumeMounts:
        - name: script-volume
          mountPath: /scripts
          readOnly: true
        - name: logs-volume
          mountPath: /var/log/migration

      terminationGracePeriodSeconds: 3600

      volumes:
      - name: script-volume
        configMap:
          name: {{ .Values.configMap.name }}
          defaultMode: 0755
          optional: false
      - name: logs-volume
        emptyDir:
          medium: {{ .Values.volumes.logs.medium | quote }}
          sizeLimit: {{ .Values.volumes.logs.sizeLimit }}
{{- end }}
