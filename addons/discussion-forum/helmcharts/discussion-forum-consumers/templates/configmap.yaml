apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.names.fullname" . }}
  namespace: {{ .Release.Namespace }}
data:
  kong-consumers.yaml: |
{{- if .Values.configmap.enabled -}}
{{ tpl (.Files.Get "configs/kong-consumers.yaml") . | nindent 4 }}
{{- end -}}
